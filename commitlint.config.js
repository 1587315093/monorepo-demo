module.exports = {
  // 继承了 @commitlint/config-conventional 规则，这是一个常见的规范配置，定义了常用的提交类型（feat、fix、docs、style、refactor、test、chore、revert）和一些约定。.
  // commitlint 的文档：https://commitlint.js.org/#/reference-rules
  extends: ["@commitlint/config-conventional"],
};
